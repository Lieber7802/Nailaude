# M3_2 CLI Adapters Plan

## Goal
Add bounded CLI process execution and deterministic adapter fallback.

## Scope
- `ProcessPool`, Codex/OpenCode adapters, adapter manager, CLI research notes.

## Tests
- Normal output, timeout, cancellation, non-zero exit, health and fallback.
