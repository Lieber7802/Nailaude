# M3_4 Project State Plan

## Goal
Scan workspaces safely, inspect Git with short timeouts, and expose project state.

## Tests
- Sensitive filtering, truncation, Git fallback, refresh warnings, GET API.
