# M3_8 Runtime Plan

## Goal
Execute queued runs in batches with snapshots, cancellation, failure blocking, and status snapshots.

## Tests
- FIFO, queue limit, parallel batch execution, cancellation, warning completion, reconnect snapshot.
